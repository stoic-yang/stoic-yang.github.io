---
title: "2022 ICPC 西安"
date: 2025-10-12 20:17:56
updated: 2025-10-12 20:17:57
zhihu_url: "https://zhuanlan.zhihu.com/p/1960800874415781262"
zhihu_id: "1960800874415781262"
---
## J

[https://codeforces.com/gym/104077/problem/J](https://codeforces.com/gym/104077/problem/J)

签到题，仔细观察发现最多选两个数，那选最大的两个非负数就行了

## F. Hotel

*贪心*

签到题，对于每一个队伍进行贪心的选择就可以了，这里我思考不周，漏写了一种情况导致 WA 了一发，qwq，而且我写还比较一坨

```cpp
#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < n; i++)
#define int long long
using namespace std;
using PII = pair<int, int>;

void solve() {
    int n; cin >> n;
    int c1, c2;
    cin >> c1 >> c2;
    vector<string> sex(n);
    for (auto &it: sex) cin >> it;
    int total = 0;
    for (int i = 0; i < n; i++) {
        int cost = min(3 * c1, 3 * c2);
        int cost2 = c1 + c2;
        if (sex[i][0] == sex[i][1] || sex[i][0] == sex[i][2] || sex[i][1] == sex[i][2]) {
            cost = min(cost, cost2);
            cost = min(cost, c2 + c2);
        }
        total += cost;
    }

    cout << total << endl;
}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int T = 1;
    // cin >> T;
    while (T--) {
        solve();
    }
    return 0;
}
```

### C . Clone Ranran

#模拟 #倍增 #贪心

不难看出，在复制前进行生产一定是亏的，所以最后的时间一定都是在进行操作二，所以先复制，再生产

**反思**

在除以一个数的时候，应该考虑这个数是否可能为 0，lz 因为没有检查这个 RE 了一发，lz 写到这里的时候大脑有点宕机，竟然没有在 10 分钟想到解法，太惭愧了，严肃加训

## Problem E. Find Maximum

#dfs #暴力

找规律，队友干出来的，被狠狠带飞

```perl
#include<bits/stdc++.h>
using namespace std;
#define ll long long
ll f(ll x){
        if(x<0)return -1;//防止出问题
        if(x==0)return 1;
        if(x%3==0)return 1+f(x/3);
        return f(x-1)+1;
}
ll solve(ll l,ll r){
        ll ans=-1;
        if(!l)l++,ans=1;//!!!


        ll lp=l;ll rp=r;
        while(lp%3){
                ans=max(ans,f(lp));lp++;
        }
        while(rp%3!=2 && rp>=0){
                ans=max(ans,f(rp));rp--;
        }
        ///////////////////////////
        if(rp<=lp){
                ans=-1;
                for(auto p=l;p<=r;p++)ans=max(ans,f(p));
                return ans;
        }
        else{
                return max(ans,3+solve(lp/3,(rp-2)/3));
        }
}
int main(){
        int t;cin>>t;
        ll l,r;
        for(int i=0;i<t;i++)cin>>l>>r,cout<<solve(l,r)<<endl;
        return 0;
}
```

## G. Perfect Word

#字符串 #思维 #观察

对于一个合法字符串来说，一个字符也是子串，所以一个合法的字符串肯定是一堆在给定字符串中的字符拼接而成的，假设一个长度大于 1 的字串合法，那么两个长度大于 1 的字串拼接而成的字串肯定合法，由此可以推断出，一个字串合法，肯定是由两个小于当前字串的子串拼接而成，也就是说，一个字串合法，那么它第一个字符肯定出现在集合中，最后一个字符也出现在集合中，这样拼接出来的字串合法

```cpp
#include <bits/stdc++.h>
#define int long long
using namespace std;
using PII = pair<int, int>;

void solve() {
    int n; cin >> n;
    vector<string> s(n);
    for (auto& it : s) cin >> it;
    sort(s.begin(), s.end(), [](const string a, const string b) {
        return a.size() < b.size();
    });

    set<string> st;
    int res = 0;
    for (int i = 0; i < n; i++) {
        if (s[i].size() == 1 || (st.count(s[i].substr(0, s[i].size() - 1)) && st.count(s[i].substr(1)))) {
            st.insert(s[i]);
            res = max(res, (int)s[i].size());
        }
    }

    cout << res << endl;
}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int T = 1;
    // cin >> T;
    while (T--) {
        solve();
    }
    return 0;
}
```

## L. Tree

#树 #入度 #反链 #构造

![](1960800874415781262-01.jpg)

```cpp
#include <bits/stdc++.h>
#define int long long
using namespace std;
using PII = pair<int, int>;

const int N = 1e6 + 10;
int in[N];
int n;
int fa[N];

void solve() {
    int n; cin >> n;
    for (int i = 2; i <= n; i++) {
        int x; cin >> x;
        fa[i] = x;
        in[i]++, in[x]++;
    }

    queue<int> q;
    for (int i = 2; i <= n; i++) {
        if (in[i] == 1) q.push(i);
        in[i]--;
    }

    int k = 0;
    int ans = n;

    while (q.size()) {
        int sz = q.size();
        ans = min(ans, sz + k);
        k++;
        while (sz--) {
            int u = q.front(); q.pop();
            if (!fa[u]) continue;
            in[fa[u]]--;
            if (!in[fa[u]]) q.push(fa[u]);
        }
    }

    cout << ans << endl;
}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    int T = 1;
    cin >> T;
    while (T--) {
        solve();
    }
    return 0;
}
```

参考：[https://blog.csdn.net/Euphria/article/details/132245077](https://blog.csdn.net/Euphria/article/details/132245077)
